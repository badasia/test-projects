package program;

import java.util.Scanner;

public class CLI {
/*
    public void start() {
        while (isRun) {
            if (!isWork) {
                session();
            }
        }
    }

    private void session() {
        System.out.println("Авторизуйтесь...\n");
        while (isWork) {
            Scanner in = new Scanner(System.in);
            cardNumber = in.nextLine();
            if (Bank.checkCardNumber(cardNumber)){
                System.out.println("Введите пин код:");
                pinCode = in.nextLine();
            }else{
                System.out.println("Неправельная карта!!!");
            }
        }
    }
*/
}
